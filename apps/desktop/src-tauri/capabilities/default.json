{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Enables the default desktop invoke permissions for the main control-plane window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
